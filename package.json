{
  "name": "on-device-ai",
  "version": "1.0.0",
  "description": "On-device AI web demos.",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve"
  }
}
